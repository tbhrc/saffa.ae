name: Founder approval gate
on:
  issue_comment:
    types: [created]
permissions:
  contents: read
  issues: write
jobs:
  founder-approval:
    runs-on: ubuntu-latest
    steps:
      - name: Apply founder approval or revocation
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          ACTOR: ${{ github.event.comment.user.login }}
          COMMENT_BODY: ${{ github.event.comment.body }}
          FOUNDER_LOGIN: david-aicoach
        shell: bash
        run: |
          set -euo pipefail
          body="$(printf '%s' "$COMMENT_BODY" | sed -e 's/^[[:space:]]*//' -e 's/[[:space:]]*$//')"
          [[ "$ACTOR" == "$FOUNDER_LOGIN" ]] || exit 0
          has_label() { gh api "repos/${REPO}/issues/${ISSUE_NUMBER}" --jq '.labels[].name' | grep -Fxq "$1"; }
          if [[ "$body" == "👍" || "$body" == "/approve" ]]; then has_label founder-action || exit 0; gh api --method POST "repos/${REPO}/issues/${ISSUE_NUMBER}/labels" -f 'labels[]=founder-approved' >/dev/null; gh api --method DELETE "repos/${REPO}/issues/${ISSUE_NUMBER}/labels/founder-action" >/dev/null; elif [[ "$body" == "👎" || "$body" == "/revoke" ]]; then has_label founder-approved || exit 0; gh api --method POST "repos/${REPO}/issues/${ISSUE_NUMBER}/labels" -f 'labels[]=founder-action' >/dev/null; gh api --method DELETE "repos/${REPO}/issues/${ISSUE_NUMBER}/labels/founder-approved" >/dev/null; fi